/**
 * @deprecated Use `@/components/effects/use-marquee` — kept as a thin re-export
 * so existing menu imports stay stable during the effects extraction.
 */
export { useMarquee as useMenuMarquee } from "@/components/effects/use-marquee";
export type {
  UseMarqueeOptions as UseMenuMarqueeOptions,
  UseMarqueeResult as UseMenuMarqueeResult,
} from "@/components/effects/use-marquee";
